# Environment
.env

# Python cache — all directories recursively
**/__pycache__/
**/*.pyc
**/*.pyo
**/*.pyd

# Virtual environment
venv/
.venv/

# Database
*.db

# Captured images
static/images/

# YOLO model (large binary — download separately on Pi)
*.pt

# Per-session models downloaded from the dashboard (cached by checksum)
models/

frame-selector.py
vids/*